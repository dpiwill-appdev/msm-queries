
<h1>List of all directors</h1>

<ul>
  <% @directors.each do |director| %>
    <li>
      <%= link_to "#{director.name} (DOB: #{director.dob})", director %>
      <%= link_to "Show details", director %>
    </li>
  <% end %>
</ul>
